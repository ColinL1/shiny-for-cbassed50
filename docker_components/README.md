
# UNDER CONSTRUCTION
# shiny-for-cbassed50
Shiny wrapper to process CBASS-derived PAM data efficiently and interactively

To run requires the built shiny R package shinycbassed50

Build the shiny app with

```bash
R CMD build .
```
Launch Docker with

```bash
docker compose up -d
```
